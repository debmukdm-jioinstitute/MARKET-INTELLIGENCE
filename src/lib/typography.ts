/**
 * Market Intelligence uses a single typeface everywhere.
 * @see docs/TYPOGRAPHY.md
 */
export const GOOGLE_SANS_FONT_STACK = '"Google Sans", sans-serif';

/** Inline `font-family` for HTML email and standalone pages (no Next/font). */
export const GOOGLE_SANS_FONT_FAMILY_CSS = `font-family:${GOOGLE_SANS_FONT_STACK}`;
